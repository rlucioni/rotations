[flake8]
max-line-length = 119
exclude =
    # No need to traverse the git directory.
    .git,
    # No need to check cache directories.
    __pycache__,
    # Migrations are autogenerated.
    migrations,
    # No need to check installed, third-party packages.
    venv
